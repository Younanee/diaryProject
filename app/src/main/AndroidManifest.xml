<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nyoun_000.diaryproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.main_view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.init_view.InitViewActivity"
            android:label="@string/title_activity_init"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".InitViewActivity"
            android:label="@string/title_activity_init_view"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>